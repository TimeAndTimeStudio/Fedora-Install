sudo dnf install git \
    meson ninja-build gcc gcc-c++ \
    pkgconf-pkg-config libdrm-devel

git clone https://github.com/intel/libva.git
cd libva
git checkout 2.24.1
meson setup build \
    --prefix=/opt/mesa \
    --libdir=lib64

ninja -C build
sudo ninja -C build install
